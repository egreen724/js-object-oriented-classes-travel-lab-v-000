class Driver {
  constructor(name, date) {
    this.name = name
    this.date = date
  }

  startDate(this.date) {
    new Date(this.date)
  }

  yearsExperienceFromBeginningOf(endYear) {
    return endYear - startDate.year
  }
}
